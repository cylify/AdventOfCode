#include <iostream>
#include <iterator>
#include <set>
#include <string>
#include <algorithm>
#include <fstream>
#include <vector>

using namespace std;
typedef long long ll;

int fight(string battle) {
}

int main() {
    ifstream day2("day2Input.txt");
    string battle;
    while(getline(day2, battle)) {

    }
}